%p= @breadcrumb 

%fieldset
  %legend
    = @unit.fullname
    = link_to 'quiz', quiz_unit_path(@unit)
    = link_to 'edit', edit_unit_path(@unit)
  %fieldset
    %legend Exercises
    %ol
      - @unit.exercises.each do |exercise|
        %li{ :value => exercise.number }= exercise.description

